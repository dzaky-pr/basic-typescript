let myName: string = "Dzaky";
let meaningOfLife: number;
let isLoading: boolean;
let album: any;

myName = "Dzaky Muhammad";
meaningOfLife = 42;
isLoading = true;
album = 1998;

const sum = (a: number, b: string) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
